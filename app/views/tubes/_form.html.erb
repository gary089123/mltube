<%= form_with(model: tube, local: true) do |form| %>
  <% if tube.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(tube.errors.count, "error") %> prohibited this tube from being saved:</h2>

      <ul>
      <% tube.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title, id: :tube_title %>
  </div>

  <div class="field">
    <%= form.label :transcript %>
    <%= form.text_area :transcript, id: :tube_transcript %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
